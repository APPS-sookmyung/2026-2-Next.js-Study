# Next.js 개요

### Next.js = 리액트 전용 웹 개발 프레임워크

- react.js + 페이지 라우팅, 빌트인 최적화 기능, 다이나믹 HTML 스트리밍 등 리액트를 보다 더 강력하고 편하게 사용할 수 있는 기능들을 제공

### Next.js는 Library가 아닌 Framework

- React.js = UI 개발을 위한 JS “Library”
- Next.js = React.js 전용의 웹 개발 “Framework”
- 개발 구현의 주도권이 누구에게 있는가
    - 라이브러리 : 개발자에게
        - 기능 구현을 원하는 방향으로 진행, 개발자가 자유롭게 구현 가능
        - 거의 모든 기능을 제공 - 더 빠르고 편리하게 개발 가능!
    - 프레임워크 : 개발자 X
        - 주도권을 프레임워크가 가짐, 프레임워크가 제공하는 기능을 이용하거나 허용하는 범위 내에서만 추가 도구 사용 가능
        - 기본 기능 외 제공 x

# 사전 렌더링

- 브라우저의 요청에 사전에 렌더링이 완료된 HTML을 응답하는 렌더링 방식
- 기존 리액트가 가진 Client Side Rendering의 단점을 효율적으로 해결하는 기술

### Client Side Rendering

- React.js 앱의 기본적인 렌더링 방식
- 클라이언트(브라우저)에서 직접 화면을 렌더링하는 방식

1. 사용자가 브라우저를 통해 서버에 초기 접속 요청을 보냄
2. 서버는 index.html(빈껍데기) 파일을 브라우저에 전송
3. 브라우저는 빈 화면을 렌더링 (사용자가 보는 화면 - 아무것도 나오지 않음)
4. 서버는 react 앱을 JS Bundle 형태로 브라우저에 전송
5. 브라우저가 리액트 앱을 실행해 화면에 컨텐츠가 렌더링됨

- 장점 : 초기 접속 이후 일어나는 페이지 이동이 매우 빠르고 쾌적
    - JS Bundle - 해당 서비스에서 접근 가능한 모든 컴포넌트 코드가 존재하므로 서버에게 새로운 페이지를 요청할 필요가 없음
    - 브라우저에게 페이지 요청 이동을 보내면 브라우저가 자체적으로 js를 실행해 페이지 이동을 맡음
- 단점 : 초기 접속 속도가 느리다
    - FCP(First Contentful Paint) - 요청 시작 시점으로부터 컨텐츠가 화면에 처음 나타나는데 걸리는 시간
    - 브라우저가 화면을 렌더링할때까지 필요한 과정이 많아 FCP 증가
        - index.html 파일 받기, js bundle 받기, js 실행 - 브라우저가 할 일이 너무 많다!

### 사전 렌더링

1. 사용자가 브라우저를 통해 서버에 접속 요청을 보내면
2. 서버가 직접 JS를 HTML 페이지로 미리 렌더링해 두고
3. 렌더링된 HTML 페이지를 브라우저에게 응답
    - 이 시점에서는 화면에서 상호작용 불가
4. 서버는 react 앱을 JS Bundle 형태로 브라우저에 전송
5. 브라우저가 리액트 앱을 실행해 화면에 렌더링된 HTML 코드와 연결 : 수화(Hydration)
6. TTI (Time To Interactive:첫 요청~수화까지) 상호작용 가능
- 페이지 교체는 CSR과 똑같이 동작
    - 브라우저에게 페이지 요청 이동을 보내면 브라우저가 자체적으로 js를 실행해 페이지 이동을 맡음

→ 빠른 FCP 달성 + 빠른 페이지 이동